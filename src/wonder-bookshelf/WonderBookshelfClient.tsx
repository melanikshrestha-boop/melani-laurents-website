"use client";

import { PublicBookshelf } from "./PublicBookshelf";

/**
 * Public site bookshelf entry.
 * Wonder visual language (CSS) · static titles + covers · no device sync.
 */
export function WonderBookshelfClient() {
  return <PublicBookshelf />;
}
