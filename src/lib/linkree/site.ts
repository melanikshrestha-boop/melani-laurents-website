/** @Celine_Nova — resolved once from channel page browseId/externalId. Do not fetch at runtime. */
export const YOUTUBE_CHANNEL_ID = "UCsInbSrU-XZmDvCLGR3_eTQ";

/** Hide the latest-video card if the upload is older than this. */
export const STALE_DAYS = 120;

/** Click ids allowed for analytics. New clickable elements must be listed here. */
export const ALLOWED_LINK_IDS = ["youtube-latest"] as const;
