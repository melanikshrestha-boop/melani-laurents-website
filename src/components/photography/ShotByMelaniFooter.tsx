/**
 * Photography footer chrome removed permanently (Melani: no footer stuff ever).
 * Kept as a no-op so any leftover import does not crash.
 */
export function ShotByMelaniFooter() {
  return null;
}
